# Runtime dependencies for HAKATHON- (LangGraph chatbot + Streamlit UI).
# Pin loosely — the code targets recent versions of each library.

arrow
chromadb
ddgs
langchain-community
langchain-chroma
langchain-core
langchain-google-genai
langchain-text-splitters
langgraph
# mcp >=2 is required by waggle-mcp (langchain-mcp-adapters pins <2 and is
# no longer used — bridges drive the mcp client directly via mcp_tool_bridge).
mcp>=2.0.0,<3.0.0
pydantic
pymupdf
python-dotenv
requests
streamlit
streamlit-autorefresh
fastapi
uvicorn
python-multipart
# Google OAuth 2.0 + Gmail API (per-user authentication)
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
# Waggle memory server is NOT installed here: it's a subprocess launched only
# when WAGGLE_MCP_ENABLED=true, and its dependency chain (sentence-transformers
# -> torch, etc.) exceeds Streamlit Cloud's build resources. Install it locally:
#   pipx install waggle-mcp   (requires Python 3.11+)

# Test-only
pytest
